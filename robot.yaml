tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Certification Robot" certification.robot

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
